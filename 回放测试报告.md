# DO/UNDO SQL 真实回放测试报告（2026-09-19，v1.3 精简版）

## 测试环境

| 项 | 值 |
|---|---|
| 数据库 | 便携版 PostgreSQL **12.22** Windows（与源库 Debian 12.22 同版本） |
| 实例 | 127.0.0.1:55123，库 `aphx_replay3`，862 张表（由 aphx 字典生成 DDL） |
| 输入 | `do.sql`（111,703 条，65MB）/ `undo.sql`（111,703 条，52MB） |
| SQL 特性 | DELETE/INSERT-UNDO 按主键定位；UPDATE 仅变更列（平均 2 列）；跳过无变更 UPDATE |
| 初始状态 | **空库**（WAL 窗口从中途开始，窗口前状态不可得，属正常） |

## 测试 1：DO SQL 回放

```
111,703 条语句 → 0 错误，耗时 42 秒
```

## 测试 2：UNDO SQL 回放（在 DO 结果之上）

```
111,703 条语句 → 0 错误，耗时 63 秒
```

| 表 | DO 后 | UNDO 后 | 语义 |
|---|---|---|---|
| x_actlog | 3,748 | **0** | 窗口内插入的行全部撤销 ✓ |
| e_mjck_out | 339 | **0** | 同上 ✓ |
| e_cw_ysyf | 1,256 | **18** | **窗口内被删除的 18 行"复活"——数据恢复价值证明** |
| e_ck_in | 1,748 | **17** | 同上，17 行被恢复 |

交叉验证：复活行与 WAL 中对应 DELETE 记录的旧值逐字段一致 ✓

## v1.3 精简化效果（对比 v1 全字段版本）

| 项 | v1 | v1.3 | 变化 |
|---|---|---|---|
| DO SQL 文件 | 416 MB | **65 MB** | **-84%** |
| UNDO SQL 文件 | 425 MB | **52 MB** | **-88%** |
| 语句数 | 140,489 | 111,703 | -30,668（剔除真实无变更 UPDATE） |
| DELETE WHERE | 全部列拼接 | `WHERE pk = v` | 主键定位 |
| UPDATE SET | 全部列 | 仅变更列（平均 2 列） | 增量 |

## 过程中发现并修复的问题

1. **无变更 UPDATE（30,668 条）**：e_ck_in 等表的批量作业重写相同值（`SET x=x`），
   v1 生成了无意义的空转语句，v1.3 正确跳过（重放语义安全）——字节级验证确认新旧值完全一致；
2. **页像重放引擎缺陷**：物理重放（FPI 播种/元组 redo）曾只对已提交事务执行，
   导致页像漂移、一条 UPDATE 的旧值取错（产生虚假的主键变更语句）。修复为物理重放对
   所有记录执行（页是物理的），仅变更收集按提交过滤。修复后该虚假语句消除，
   UPDATE 可执行数 +1,882，回放终态与真实语义一致。

## 附：测试实例管理

```powershell
# 连接查看
D:\mimo\pgwinal\pg12\bin\psql.exe -h 127.0.0.1 -p 55123 -U postgres -d aphx_replay3
# 停止
D:\mimo\pgwinal\pg12\bin\pg_ctl.exe -D D:\mimo\pgwinal\pgdata stop
```
