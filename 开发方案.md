# pgwinalnew 开发方案与可行性评估

| 项目 | 内容 |
|------|------|
| 文档版本 | v1.2（明确 aphx 字典为唯一可用真实配对并实测核实；补充 system_id 预检、孤儿续记录、0xD101 等实测事实；测试优先级对齐生产 PG12） |
| 项目定位 | Windows 平台独立（非插件）PostgreSQL WAL 离线解析 / DML 恢复工具，带 GUI |
| 目标版本支持 | PostgreSQL 12 / 13 / 14 / 15 / 16 / 17 / 18 |
| 前置项目 | 原 pgwinal v1 实现（已失败，根因分析见 §2）——本项目为其完整重写 |
| 项目位置 | `D:\mimo\pgwinal`（git 仓库沿用原历史；WAL/字典/产物/测试实例不入库，见 `.gitignore`） |
| 参考项目 | [walminer](https://github.com/universe2000/walminer)（Linux，PG 扩展形态） |
| 本文档作用 | 开发总纲：架构、模块设计、版本策略、验证体系、里程碑、可行性结论 |

---

## 0. 结论先行（TL;DR）

> **实现状态（2026-09-19）：M0–M3 核心已全部实现并在真实数据上端到端验证通过。**
> 46 段 / 736MB / 1,722,307 条记录：帧级 CRC 与 prev 链 **100%**；产出 **146,613 条变更**，
> INSERT 可执行 **100%**、DELETE **99.7%**、UPDATE **92.5%**；DO/UNDO 各 **140,489 条**可执行 SQL 已导出。
> 开发过程中实测纠正了三处记忆错误（bimg_info 压缩位、XLOG_SWITCH 操作码、
> varlena 标签位序）——"常量必须对照官方源码核实"的纪律每一条都兑现了价值。

**可行性判定：可行，属于"高难度、中风险、强方法论依赖"项目。**（已由实现验证）

1. **旧项目 pgwinal 的失败不是方向错误，而是实现方法论错误**：它没有做 CRC 校验、二进制布局常量靠手抄（多处抄错）、只用合成 WAL 自测（自己造的数据当然能解析通过）、出错后用"值可信度打分"等启发式兜底掩盖问题。这条路线必然失败，且已经失败。
2. **walminer 证明了这条路是通的，且市场空缺真实存在**：开源的 [walminer 3.x](https://gitee.com/movead/XLogMiner)（MIT 协议，稳定支持 PG10–12）已在生产使用多年，其源码已 clone 到 `reference/XLogMiner` 供直接参考；闭源的 [walminer.cn](http://walminer.cn/)（4.x）支持 **PG 10–18** 且可正常恢复数据，但**仅 Linux**。**Windows + 开源 + PG12–18 的组合无人占位**——独立多版本 WAL 解析器在技术上成立，难点全部在工程细节。
3. **本方案的核心转变：从"尽力解析"转为"正确性优先（correctness-first）"**——每条 WAL 记录必须通过 CRC32C 校验才算数；每个二进制布局常量必须从官方头文件生成而非手抄；每条生成的 SQL 必须带"可执行性"标记，不确定的进"待人工复核"区，绝不静默输出错误 SQL。
4. **推荐技术路线**：Python 3.11+ 实现（核心解析 + CLI + GUI 共用），PySide6 做 GUI（继承旧项目界面布局），SQLite 存字典与结果，psycopg2 连库建字典；CRC32C / lz4 / zstd 用 C 加速包可选。备选路线（C 移植 PG 源码、pg_waldump 子进程、恢复实例+逻辑解码）见 §5.1 对比表。
5. **工作量估算**：到可日常使用的 M4 里程碑约 **8–12 人周**（熟悉 PG 内部结构的前提下）。M0（1 周）是关键去风险关卡：真实 WAL 上与 `pg_waldump` 逐条对照达到 100% 帧级一致才继续，否则切换备选路线。
6. **必须诚实面对的边界**（与 walminer 相同的物理约束，详见 §4）：
   - `wal_level=replica` 时：INSERT 的 DO/UNDO 完整；UPDATE/DELETE 的 UNDO 旧值不在 WAL 记录里，需靠 FPI（全页镜像）或流内历史重建，不保证 100%。
   - `wal_level=logical` + `REPLICA IDENTITY FULL`：UPDATE/DELETE 的 DO/UNDO 完整。
   - DDL（改列类型/表重建/drop）之后，旧 WAL 的 relfilenode 无法映射到新结构 → 需要旧时点字典快照或"替身表"映射（学习 walminer 的 avatar 机制）。

---

## 1. 需求与验收标准

### 1.1 需求映射

| # | 原始需求 | 本方案落点 | 置信度 |
|---|---------|-----------|--------|
| 1 | 解析 WAL 得到 DO/UNDO SQL，可恢复数据 | §6.2/6.3/6.6：严格解码 + SQL 生成器 + 可执行性分级 | 高 |
| 2 | 支持 PG 12–18 | §7：版本档案系统（常量从官方头文件生成）+ 真实实例测试矩阵 | 高（12–17）/ 中（18 需实测） |
| 3 | 学习 walminer，解析数据入解析表 | §3/§6.7：结果表对齐 `walminer_contents` 并增强 | 高 |
| 4 | 独立程序，非插件 | 纯用户态文件解析，不依赖 PG 安装（建字典时才连库） | 高 |
| 5 | 支持 Windows | Python + PySide6 + PyInstaller 单文件 exe | 高 |
| 6 | GUI：添加 WAL、生成字典、操作方便 | §6.8：继承旧项目布局 + 新增预检/字典时间线/导出向导 | 高 |
| 7 | 结构变化（DDL）下能否生成恢复 SQL | §8：字典时间线 + 替身解析 + 目录自举研究 | 中（分场景，见 §8.5 边界） |
| 8 | 界面参考 pgwinal | §6.8：沿用双行工具栏/左右分栏/底部日志的布局约定 | 高 |

### 1.2 "可执行 SQL" 的验收定义（需求 1 的核心）

一条 DO/UNDO SQL 称为**可执行**，当且仅当同时满足：

1. 所在 WAL 记录通过 CRC32C 校验（帧级完整）；
2. relfilenode 成功映射到字典中的表（含替身/时间线解析）；
3. 所有列按类型正确解码，无 hex 回退、无截断、无 TOAST 缺失；
4. UNDO 的旧值来源明确（WAL 记录内 / FPI 重建 / 流内历史，三选一，见 §4.2）；
5. 生成的 SQL 在同结构空库上执行不报语法/类型错误。

不满足任一条的记录进入结果表但标记 `executable=false` + 原因，导出时单独分区（"待人工复核"），**绝不与可执行 SQL 混排**。这是与旧项目的根本区别：旧项目把猜测值直接拼进 SQL。

---

## 2. 旧项目 pgwinal 失败根因分析

### 2.1 代码级证据（本次代码审查发现）

对 `D:\mimo\pgwinal` 的核心代码（`core/xlog.py`、`parse/engine.py`、`core/heaptuple.py`、`constants.py`）审查，确认以下问题：

| # | 位置 | 问题 | 后果 |
|---|------|------|------|
| 1 | `xlog.py` `_XLOG_PAGE_FMT="<HHIQII"` | 页头按 6 字段 24 字节解析，但真实 `XLogPageHeaderData` 只有 5 个有效字段（magic/info/tli/pageaddr/rem_len，20 字节 MAXALIGN 到 24）。代码把**第 5 个字段（rem_len）当成了 `xlog_blckseg`，把第 6 个字段（对齐填充的垃圾字节）当成了 `xrecoff` 并用作续页长度** | 跨页长记录（大事务、COPY）拼接全部错误 |
| 2 | 全局 | **没有实现 CRC32C 校验**（`strict_crc=False`），记录边界靠"合理性猜测"（`rmid<=200`、`xid<0xFFFFFFFF`），不满足就 `off += 8` 盲跳重同步 | 错位后产生大量伪记录/丢记录，且**无任何报错** |
| 3 | `constants.py` `VARATT_1B=0x80` | varlena 标志位错误。真实规则是首字节低 2 位：`00`=4B 压缩、`01`=4B 未压缩、`10`=1B、`11`=1B 外部（TOAST） | 所有变长字段（text/varchar/bytea...）解码错位 |
| 4 | `xlog.py` `_split_body` 镜像头 | 按 `hole_offset,image_length` 顺序读 5 字节；官方 `XLogRecordBlockImageHeader` 实为 **`length` 在前、`hole_offset` 在后**（已对照 PG12–16 头文件确认），且 FPI 镜像数据读出后**从未使用** | FPI（UNDO 旧值的关键来源）完全缺失 |
| 5 | `engine.py` `_decode_multi_insert` | 忽略了 main data 中 `flags+ntuples` 之后的 `offsets[]` 数组（walminer 逐条读 `xlrec->offsets[i]`），offnum 用循环序号 `i` 代替 | 批量插入（COPY）行定位错误 |
| 6 | `heaptuple.py`（826 行） | 存在 `_is_clean_value`、`_business_bonus`（"订单号像 SC_MJ_CK'HXMJLY 打 3 分"）等**值可信度打分启发式**；`engine.py` 把"WAL 原始可打印串"塞进 SQL 注释供人工肉眼挑 | 铁证：解码不可靠，靠猜。这是失败的方法论标志 |
| 7 | `result/gui_sim_do.sql` | 模拟测试产物只有 `BEGIN; COMMIT;` 两行 | 端到端链路实际产出为零 |

> **勘误与教训（重要）**：本方案初版曾依据记忆断言 pgwinal 的 `xl_heap_update` 14 字节布局（含 `old_xmax`）和 `MULTI_INSERT=0x50` 是错的——**经逐一核对 PG 12/13/14/15/16/17/18 官方 `heapam_xlog.h` 后证实这两处反而是对的**（`old_xmax` 在 12–18 全系存在；`MULTI_INSERT=0x50` 全系不变）。真正致命的是上表 #1/#2/#3/#4。这个插曲本身就是最好的论证：**凭记忆/手抄维护二进制常量，错误率是 100% 级别的——包括本方案作者自己**。因此"常量必须由脚本从官方头文件生成"不是工程偏好，而是本项目生存的先决条件。

### 2.2 方法论根因（比代码 bug 更重要）

1. **无校验的二进制解析 = 玄学**。WAL 是无自描述格式，没有 CRC 校验就没有"对/错"的判据，任何布局错误都会静默产出垃圾。`pg_waldump` 之所以可靠，正是因为它逐条校验 CRC、错即停。
2. **常量手抄必错**。6 个大版本的几十个结构布局靠人肉从 C 头文件转录，错误率 100%（上表 #1/#3/#4/#5/#6 均属此类）。
3. **合成测试自欺**。`tests/make_sample_wal.py` 按"自己理解的布局"造数据，再按同一套理解解析——必然通过，证明不了任何东西。真实 WAL（`testpg/` 下 46 个段）从未对照验证。
4. **启发式兜底掩盖根因**。解码错 → 加打分函数挑"看起来像业务数据"的值 → SQL 里塞原始串注释让人工挑。错误被一层层包住，直到整体不可用。

### 2.3 可继承的资产（不推倒重来）

| 资产 | 处置 |
|------|------|
| GUI 布局与交互流程（双行工具栏/左右分栏/结果着色/底部日志，1440×900） | **整体继承**（需求 8），仅换框架为 PySide6 |
| 字典构建 SQL（连库拉 pg_class/pg_attribute 等） | 移植并扩展（补 LSN 时间线字段） |
| SQLite 结果表 / 字典表结构 | 移植并增强（§6.7） |
| `dict/pgwinal_dict_aphx.sqlite` + `testpg/` 46 段 WAL | **唯一可用的真实端到端验证配对**（详见 §9.4）：字典 PG12.22 / 857 表 / 17382 列，与 WAL 段头 system_id 完全匹配（已实测验证）；迁入 `testdata/` |
| `dict/` 其余 4 份字典（pgwinal_dict、tz_kqdz、tz_rjxm、tz_tzwalechat_25） | 来自另一数据库（PG12.16，同一 sysid，按 schema 过滤构建），**配套 WAL 已删除**——仅作字典导入/迁移/合并的兼容性测试素材，不能做端到端解析验证 |
| 文档体系（README/DEVELOPMENT.md 的交接模式） | 沿用结构 |
| `collect_wal_files` 文件收集逻辑 | 移植 |

---

## 3. walminer 机制研究与借鉴

### 3.1 walminer 的核心原理（必须继承的设计）

研究 walminer 3.x 源码行为与文档（[GitHub](https://github.com/universe2000/walminer)、[digoal 详解](https://github.com/digoal/blog/blob/aded86da45dd5b3f0f0e0634ae522f394277c2f5/202211/20221102_01.md)）得到的最重要结论：

> **walminer 的构建基础是：checkpoint 之后对每个页的首次修改会产生全页写（FPI/FPW）。因此"从某次 checkpoint 开始点 c1 起解析并记录所有 FPI，就能完美解析 c1 之后的全部 WAL"。**

这就是 walminer"精确解析"模式（`walminer_by_lsn(startlsn, endlsn, 'true')`）的原理：在用户指定范围之前先找到 checkpoint 起点，从那里积累 FPI 重建页状态。**FPI 不是可选增强，而是 UPDATE/DELETE 旧值重建（即可执行 UNDO）的地基**——旧项目完全没有实现 FPI，等于放弃了 walminer 最核心的机制。本方案将 FPI 挖掘列为一等公民模块（§6.3.4）。

### 3.2 walminer 功能与结果模型（对齐需求 3）

- 工作流：`wal_add → build/load dictionary → walminer_by_lsn/time/xid → 查 walminer_contents → walminer_stop`
- `walminer_contents` 字段：`sqlno, xid, topxid, sqlkind, minerd, timestamp, op_text, undo_text, complete, schema, relation, start_lsn, commit_lsn`
- 特色功能：
  - **精确解析**（先找 checkpoint 再解析，保证完整）
  - **单表解析**（reloid 过滤）
  - **替身解析**（`walminer_table_avatar`）：表被 drop/truncate 后，新建同结构"替身表"映射到丢失的 relfilenode，旧 WAL 即可解析 → 本方案 §8.3 直接借鉴
  - **数据页挽回**（`page_collect`）：从 WAL 搜页镜像修坏块/挽回 TRUNCATE 数据 → 本方案 P3 研究项
  - 系统表变化解析（`wal2sql_with_catalog`）/ DDL 解析（`wal2sql_with_ddl`）

### 3.3 walminer 的约束（我们同样要面对的物理边界）

来自 walminer 官方 README/文档：

1. 只解析 DML（DDL 解析长期开发中）；DDL 相关操作（删表/truncate/改表空间/改列类型）之后，相关 DML **解析不出**；
2. 只能解析与数据字典**时间线一致**的 WAL；字典无法处理的 relfilenode 记录被**丢弃**（仅 notice）；
3. WAL 有缺口则该时段 SQL 丢失；
4. ctid 是"当时"的值，vacuum 后不可靠，undo 不能盲目直接执行；
5. 旧版 xlogminer 要求 `wal_level=logical` + `replica identity FULL` 才能解析 update/delete；walminer 3.x 放宽到不依赖 logical，但完整 UNDO 仍受 replica identity 影响；
6. 解析库与被解析库需同版本（场景二）——**独立程序形态（本项目/walminer 4.0 方向）可摆脱此限制**。

### 3.4 两条参考路径（2026-09 更新，含源码级研究）

| | 路径 1：walminer 3.x（开源） | 路径 2：Walminer 4.x（walminer.cn） |
|---|---|---|
| 地址 | https://gitee.com/movead/XLogMiner | http://walminer.cn/ |
| 形态 | PG 扩展（C 源码，**MIT 协议**） | 独立 bin 工具（**闭源**） |
| 版本支持 | 官方声明稳定支持 PG 10–12；master 源码 Makefile 声明"支持 PG 10–19"，含 PG13–17 编译兼容层（`wm_compatible.c` 等），但高版本未经官方回归 | **PG 10–18**，可正常恢复数据 |
| 平台 | Linux（扩展形态依赖 PG 服务端） | **仅 Linux** |
| 功能 | DML 解析 + UNDO、精确解析、替身、页挽回、spill、（开发中）DDL | DML+DDL 解析、UNDO、CDC 解码插件、字典生成、WAL 搜索、快速表恢复（备份集+WAL）、单表并行导出、**数据脱库**（库起不来时 dump 数据）、多衍生库兼容（PolarDB/openGauss/HaloDB/BabelFish） |

**结论：市场空缺再次确认**——能正常恢复数据的两个实现，一个开源但止步 PG12 且是 Linux 扩展形态，一个支持 10–18 但闭源且仅 Linux。**Windows + 开源 + 12–18 的组合无人占位**，本项目定位成立。

### 3.5 walminer 3.x 源码研究成果（已 clone 至 `reference/XLogMiner/`）

源码已完整 clone 到 `D:\mimo\pgwinalnew\reference\XLogMiner`（MIT 协议，**允许直接参考/移植代码**，需保留版权声明）。核心文件约 1 万行 C，精读结论：

**架构（`walminer_decode.c`，1847 行）**：

1. **搜索线程 + 解析线程**：搜索线程维护 checkpoint 链表、负责精确模式前探与 LSN 定位；解析线程在 LSN 确定后开始解码。`WalminerSearchWal`/`WalminerAnalyseProcess` 两个状态机分离。
2. **页像存储（imagemanage.c）本质是"堆页 WAL 重放引擎"**——这是 walminer 最核心的机制，比"FPI 提取"更进一步：
   - FPI 播种：checkpoint 后每页首次修改的 FPI 存入 `(relfilenode, blkno) → page` 哈希；
   - **每条 DML 记录向页像 redo**：无 FPI 的 INSERT/UPDATE 用 `PageAddItem` 把新元组写进页像；DELETE 设 `PageSetPrunable`；
   - **prune/vacuum 记录也必须 redo**（`heap_page_prune_execute_walminer` 重实现行指针 redirect/dead/unused + `PageRepairFragmentation`），否则页像与真实页漂移，后续从页像取的旧值就是错的——这是 FPI 重建正确性的隐蔽关键点；
   - 带 FPI 的记录**直接从页像读元组**（`PageGetItemId`+`PageGetItem`），此时记录里没有元组数据；
   - UPDATE 的 `PREFIX/SUFFIX_FROM_OLD` 差量**必须从旧页像取旧元组拼接**，取不到则整条 UPDATE 放弃解析。
3. **事务模型**：`TransactionEntry` 链表（含 topxid 子事务归并、`first_sure_xid` 完整性判定）；大事务 spill 落盘（`xid_N.spill`，`MAX_CHANGES_IN_MEMORY=4096` 触发）。
4. **过滤**：`filter_in_decode` 按字典 dboid + relfilenode→reloid 解析过滤，只关心 `RELKIND_RELATION/PARTITIONED_TABLE/TOASTVALUE`；解析不到的 relfilenode 记入 `missd_relfilenode` 并丢弃（仅 NOTICE）。
5. **版本兼容实证**：源码 `#if PG_VERSION_NUM` 分支覆盖 PG10–19（含 PG17 prune 三 opcode、PG14 VACUUM/PRUNE 拆分、PG19 `RM_XLOG2_ID` 预留）——**单代码库多版本解析在 C 层面已被 walminer 验证可行**，差异全部集中在少数结构体与 opcode 上。

**可直接复用的资产**：

| 资产 | 用途 |
|------|------|
| `expected/wal2sql_*.out`（14 组回归测试期望输出） | **现成的黄金测试用例**：base/subxid/lsn/time/xid/missimage/toast/spill/singletable/type/avatar/self_apply/ddl |
| `sql/wal2sql_*.sql` | 测试剧本，直接改造为本项目真实实例矩阵的剧本 |
| `datadictionary.c`（1139 行） | 字典构建/存储/替身映射的表结构与查询逻辑参考 |
| `imagemanage.c`（480 行） | 页像重放引擎的算法参考（Python 重写） |
| `wal2sql.c`（1944 行） | DO/UNDO SQL 生成规则参考（含主键 WHERE 策略） |
| `walminer_contents` 表结构 | 结果表对齐基准 |
| `todo.md` 限制清单 | 化身表不支持 TOAST、DDL 解析需精确模式等边界，直接继承为已知限制 |

**walminer.cn（4.x 闭源版）功能清单的启示**（作为远期路线图参考）：WAL 搜索（定位指定表操作位置）、快速表恢复（备份集+连续 WAL 恢复单表，减小 RTO）、数据脱库（库无法启动时 dump 数据）、CDC 解码插件（replica 级全库解码）——这些证明 WAL 解析内核建好后，上层产品化空间很大。本项目 M4 之前不纳入，仅记录。

---

## 4. 关键技术约束矩阵（"可执行 SQL"的物理边界）

### 4.1 wal_level × replica identity 决定 WAL 里有什么

| 配置 | INSERT | UPDATE | DELETE | 说明 |
|------|--------|--------|--------|------|
| `wal_level=minimal` | 新元组 ✓ | 新元组 ✓（旧值仅当键变更时记录） | 无旧行像（仅 offnum） | **不支持**：大量信息不落 WAL，本工具要求 ≥ replica |
| `wal_level=replica` + 默认 REPLICA IDENTITY（PK） | 新元组 ✓ | 新元组 ✓；旧值不记录 | 旧行像不记录（仅键变更场景例外） | DO 完整；**UNDO 旧值需 FPI/流内历史重建** |
| `wal_level=replica` + `REPLICA IDENTITY FULL` | 新元组 ✓ | 新+旧完整元组 ✓ | 完整旧元组 ✓ | DO/UNDO 完整（walminer 3.x 的适用场景） |
| `wal_level=logical` + FULL | 同上 ✓ | 同上 ✓ | 同上 ✓ | 同上，且支持逻辑解码交叉验证 |

> 结论写进产品文档与 GUI 预检：**要 100% 可执行 UNDO，建议源库 `REPLICA IDENTITY FULL`**；否则 UPDATE/DELETE 的 UNDO 走"FPI 重建 + 流内历史"尽力路径，结果表标注 `undo_source` 与置信度。

### 4.2 UNDO 旧值的三条来源（按优先级）

1. **记录内**（record-embedded）：`XLH_UPDATE_CONTAINS_OLD_TUPLE/KEY`、`XLH_DELETE_CONTAINS_OLD_*` —— 配置决定，最可靠；
2. **FPI 重建**（fpi）：checkpoint 后该页首次修改的 FPI 含修改前页像 → deform 出旧行。覆盖率高（每次 checkpoint 后每页至少一个 FPI），是精确解析的地基；
3. **流内历史**（history）：同一 WAL 流中该 (relfilenode, ctid) 更早的 INSERT/UPDATE 新值。旧项目的 `_tuple_images` 思路正确但实现被上游解码错误污染，保留该设计。

### 4.3 结构变化的影响（需求 7 的边界预览，详见 §8）

WAL 只含物理行变更，**不含 ALTER TABLE 语句**。结构变化的影响本质是"relfilenode ↔ 列布局"映射在时间轴上发生了分叉。应对 = 字典时间线（多快照按 LSN 选择）+ 替身映射 + 目录自举（研究）。**无法做到的**：列被改类型且无旧字典时，旧值只能 hex 呈现。

---

## 5. 总体架构

### 5.1 技术选型与备选路线对比

| 路线 | 描述 | 保真度 | Windows 成本 | 版本矩阵成本 | 开发效率 | 判定 |
|------|------|--------|-------------|--------------|----------|------|
| **A. Python 独立实现（推荐）** | 纯 Python 严格重写 WAL 核心；PySide6 GUI；SQLite；psycopg2 建字典 | 高（取决于验证体系） | 低（PyInstaller 单 exe） | 中（版本档案+生成常量） | 高 | ✅ **采用** |
| B1. C 移植 PG 源码 | 拿 xlogreader.c/heap 解码等源码编独立 bin（walminer 4.0 路线） | 最高 | 高（MSVC 工具链） | 高（6 版本 #ifdef 或 6 个 bin） | 低 | 备选（性能档/Plan B） |
| B2. pg_waldump.exe 子进程 | PG Windows 发行版自带 pg_waldump.exe | 仅帧级（拿不到元组值） | 零 | 零 | — | ❌ 不能当核心；✅ **用作对照验证与预检**（§9.2） |
| B3. 恢复实例 + 逻辑解码 | 起临时实例用 wal2json 出 SQL | 高 | 高（需基础备份+实例） | 高（需每版本实例） | — | ❌ 非离线场景；文档中作为互补建议 |
| B4. 直接用 walminer 4.x（walminer.cn） | 商业闭源 bin | 高 | 高（Linux only） | — | — | ❌ 不满足 Windows/开源/可控性 |

> 补充：walminer 3.x 源码为 **MIT 协议**，路线 A 将其作为**算法参考实现**逐模块对照（页像重放、字典、SQL 生成），必要时可直接移植代码片段（保留版权声明），这是路线 A 保真度的重要保障。

**选 A 的理由**：旧项目失败与语言无关（是方法论失败）；Python `struct` 完全胜任精确二进制解析；类型系统/GUI/字典工具链开发效率最高；性能对"恢复场景"（几个段~几 GB）足够（§11.2）；接口分层清晰，未来可无痛换 Rust/C 内核（PyO3）而不动上层。

**技术栈**：Python ≥3.11；PySide6（GUI）；SQLite（字典+结果）；psycopg2-binary（建字典，可选）；`crc32c`（C 加速，可选，纯 Python 回退实现内置）；`lz4`/`zstandard`（TOAST/FPI 压缩解码，可选）；PyInstaller（打包）。

### 5.2 分层架构

```
┌────────────────────────────────────────────────────────────┐
│ L7 GUI (PySide6)      预检 / 字典时间线 / 解析 / 结果 / 导出  │
│ L7' CLI               同核心的命令行入口（CI/脚本用）         │
├────────────────────────────────────────────────────────────┤
│ L6 结果存储           SQLite: contents / runs / unresolved  │
│ L5 SQL 生成器         DO/UNDO + 可执行性分级 + 导出           │
│ L4 数据字典           构建 / LSN 时间线 / 替身 / JSON 往返    │
├────────────────────────────────────────────────────────────┤
│ L3 类型系统           OID→SQL 字面量（~45 类型 + 数组 + 回退） │
│ L2 Heap DML 解码器    INSERT/UPDATE/DELETE/MULTI_INSERT      │
│   ├─ 页像重放引擎     FPI 播种 + DML/prune redo（UNDO 地基）  │
│   ├─ TOAST 重组器     chunk 收集 / pglz / lz4 / zstd         │
│   └─ 事务过滤器       commit/abort/子事务/两遍扫描/spill      │
├────────────────────────────────────────────────────────────┤
│ L1 记录解码器         XLogRecord → blocks + main（版本档案）  │
│ L0 段/页读取器        页头/长头/续页/CRC32C 严格校验          │
└────────────────────────────────────────────────────────────┘
        输入: WAL 段文件 (+ 可选 pg_control)     输出: SQLite + .sql + xlsx
```

### 5.3 数据流（对齐 walminer 心智 + 精确解析）

```
添加 WAL 文件/目录 → [预检] 段连续性/时间线/版本探测/CRC 抽检
→ 生成/导入字典（可多份，带 LSN 时间线）
→ 解析:
   Pass0 (可选, 精确模式): 定位目标范围之前最近 checkpoint 起点 c1
   Pass1: 全量扫描 → commit/abort xid 集合 + FPI 页快照库 + TOAST chunk 库
   Pass2: Heap/Heap2 DML 解码 → 字典映射 → 类型渲染 → DO/UNDO
→ 结果入 SQLite (walminer_contents 同构表)
→ 过滤查看 / 导出 DO SQL / UNDO SQL（逆 LSN 序）/ Excel
```

---

## 6. 核心模块详细设计

### 6.1 L0 段/页读取器（正确性的第一道闸）

- 段文件识别：24 位 hex 名 / `.partial` / 自定义名；**段大小自动探测**：读段首长页头的 `xlp_seg_size`（旧项目硬编码 16MB 且从未读该字段）；支持 `pg_control` 文件（可选输入）直接取版本/段大小/blcksz/wal_level/system_id。
- 页头解析：短头 24B（MAXALIGN(20)），长头 40B（MAXALIGN(36)，含 sysid/seg_size/blcksz）；页 magic 校验（**0xD101**，12–18 一致，实测 aphx 真实段确认）；`XLP_FIRST_IS_CONTRECORD` 续页用**页头内 `xlp_rem_len`**（offset 16，修复旧项目 bug #1）；**中途起始的段集**（如 testpg 批次：首段第一条即跨段续记录、无前文可拼）按"孤儿续记录"处理——跳过 rem_len 字节并记入预检报告，不报错不猜测。
- **system_id 一致性校验**：段头 `xlp_sysid` 必须与字典 `meta.system_id` 一致（aphx 配对已实测验证此机制），不一致 = 字典与 WAL 来自不同数据库，预检直接拦截——这是防止"拿错字典解析错数据"的第一道业务闸门。
- **CRC32C 严格校验**：实现与 `xlogreader.c` `DecodeXLogRecord` 相同的校验（整条记录，xl_crc 字段以 0 参与计算）。**校验失败 = 硬停 + 精确 LSN 报告**（对齐 pg_waldump 行为）。删除旧项目的 `off += 8` 盲跳重同步。
- 零页/未初始化页、段尾填充的识别与跳过。
- 退出标准：任意真实 WAL 段上，记录条数/LSN/xid/rmid/info 与 `pg_waldump` 100% 一致（§9.2）。

### 6.2 L1 记录解码器 + 版本档案系统

- `XLogRecord` 24B 头（12–18 稳定）→ 块引用链（4B 头 + 可选镜像头/rellocator/blkno/镜像数据/块数据）→ main data（0xFF/0xFE 标记）→ origin(0xFD)/toplevel-xid(0xFC)。
- **版本档案（VersionProfile）**：`pgwal/profiles/v12.py ... v18.py`，全部由 `tools/gen_constants.py` 从 postgres 官方源码 tag（REL_12_STABLE…REL_18_STABLE 的 `xlogrecord.h`/`heapam_xlog.h`/`rmgrlist.h` 等）**生成**，禁止手写。生成物进版本库，CI 校验与头文件一致。
- 版本探测优先级：`pg_control` > 字典 `server_version` > 长头 seg_size/CRC 引导探测（对候选档案逐一试解析，取首个连续 N 条 CRC 全过的档案）。

### 6.3 L2 Heap DML 解码器

#### 6.3.1 记录级解码（以 PG 源码为规格）

每个 opcode 的 main/block 布局**以对应版本 `heapam.c` 中 `log_heap_*` 的 `XLogRegister*` 调用序列为实现规格**，逐版本单测锁定：

- INSERT（`offnum+flags`；新元组在 block0 data，含 5B `xl_heap_header`）
- UPDATE / HOT_UPDATE（10B main：old_offnum/old_infobits/flags/new_xmax/new_offnum；旧元组按 flags 在 main 或旧块 data；新元组支持 PREFIX/SUFFIX 压缩差量）
- DELETE（8B main：xmax/offnum/infobits/flags + 可选旧元组/旧键）
- MULTI_INSERT（main：flags+ntuples+`offsets[]`；block data：逐条 `xl_multi_insert_tuple`，7B 头 + datalen，MAXALIGN 对齐）
- 事务：COMMIT/ABORT/PREPARED（含 commit_ts 提取，`xact_time` 为 2000 纪元微秒）；子事务经 `XLR_BLOCK_ID_TOPLEVEL_XID` 归并到顶层 xid（对齐 walminer 的 topxid 字段）。

#### 6.3.2 元组 deform（heaptuple 重写）

- `HeapTupleHeader` 23B（xmin/xmax/cid/ctid(6B)/infomask2/infomask/hoff）+ null bitmap（HASNULL）+ 可选 OID（HASOID，仅旧元组场景）+ 用户数据；
- 列对齐严格按字典的 `typalign`（b/s/i/d）与 `typlen`（-1/-2 变长）；**varlena 标签按首字节低 2 位**（00=4B 压缩 / 01=4B 未压缩 / 10=1B / 11=1B 外部）——修复旧项目 bug #3；
- `attisdropped` 列按 NULL 占位保持位序；系统列不输出；
- 删除一切"可信度打分"启发式：解码要么成功要么抛 `DecodeError`（带 LSN 定位）。

#### 6.3.3 事务过滤

两遍扫描（Pass1 收集 commit/abort xid 与 commit_ts；Pass2 只输出已提交，可选项放开）；prepared 事务按 PREPARE+COMMIT_PREPARED 配对；walminer 同款限制记录在案：xid 过滤模式不支持子事务（输出层用 topxid）。

#### 6.3.4 页像重放引擎（UNDO 地基，对齐 walminer imagemanage 机制）

源码研究证实 walminer 的 image 存储不是"提取 FPI 存快照"，而是一个**持续 redo 的堆页状态机**。本模块按同构设计：

- **FPI 播种**：从块引用提取镜像（`length/hole_offset/bimg_info` 5B 头 + 可选 `hole_length` 2B，已核实 length 在前，修复旧项目 bug #4；HAS_HOLE 按洞偏移回填零重建 8K 页；压缩镜像 pglz 纯 Python 解压 + lz4/zstd 可选包，方法位按版本档案取值）；
- **DML redo**：无 FPI 的 INSERT/UPDATE 向页像 `PageAddItem`（按 offnum 定位）；DELETE 置 prunable——保证页像始终等于"该页在当前 LSN 的真实状态"；
- **prune/vacuum redo（正确性的隐蔽关键点）**：Heap2 的 PRUNE/VACUUM/FREEZE 记录也要应用到页像（行指针 redirect/dead/unused、`PageRepairFragmentation` 等价逻辑；PG17+ 需实现 `heap_xlog_deserialize_prune_and_freeze` 等价的子记录反序列化），否则页像与真实页漂移，取出的旧元组就是错的；
- **从页像读元组**：带 FPI 的 INSERT/UPDATE/MULTI_INSERT 记录里没有元组数据，必须从页像 `PageGetItemId`+`PageGetItem` 读取（含 lp_len）；
- **UPDATE prefix/suffix 差量拼接**：`XLH_UPDATE_PREFIX/SUFFIX_FROM_OLD` 时新元组是差量，必须从旧页像取旧元组拼出完整新值（walminer 的做法：取不到旧页像则放弃该条 UPDATE 并记录）；
- **页快照查询**：`(relfilenode, blkno) → 页像`，供 UPDATE/DELETE 的 UNDO 旧值查询（目标 LSN 处页像中同 offnum 的元组）；
- **精确解析模式（Pass0）**：定位 checkpoint 起点 c1，从 c1 起播种并持续 redo，保证目标范围可完美解析（对齐 walminer `walminer_by_lsn(...,'true')` 语义）；找不到 c1 时报错而非静默降级；
- **内存管理**：页像哈希设 LRU 上限；大事务 change 列表超阈值 spill 落盘（对齐 walminer `xid_N.spill` 机制，阈值可配）；
- **物理重放与变更收集分离（v1.3 实测修复）**：FPI 播种与元组 redo 必须对**所有**记录执行（含未提交/已回滚事务——页是物理的，未提交元组同样占位），仅变更收集/流内历史按提交过滤。实测曾因跳过未提交记录的物理重放导致页像漂移、一条 UPDATE 的旧值取错（产生虚假的主键变更语句），修复后消除。

#### 6.3.5 TOAST 重组器

- 字典含 `reltoastrelid` 映射；TOAST 表的 heap 记录按普通 heap 解码（chunk_id/chunk_seq/chunk_data）；
- 外部 varlena（标签 11）按 chunk_id 聚合、chunk_seq 排序拼接；
- 解压：pglz 纯 Python；lz4/zstd 可选包；未解压/缺 chunk → 标记 `toast_incomplete`，该行 `executable=false`。

### 6.4 L3 类型系统（SQL 字面量渲染）

- 覆盖：bool/int2/4/8/oid/float4/8/numeric(Decimal 精确)/text/varchar/bpchar/bytea(hex)/name/char/uuid/json/jsonb/date/time/timetz/timestamp/timestamptz/interval/inet/cidr/macaddr/枚举(经字典)/常见一维数组；
- 时间类严格按 2000-01-01 纪元（timestamp=µs，date=天，interval=µs+days+months）；
- 标识符 `""` 转义、字符串 `E''` 转义、NULL、true/false；
- 未覆盖类型：`decode('...','hex')` 字面量 + `executable=false`（明确提示人工复核），**不再猜测**；
- 渲染器与解码器分离，单测以 PG 真实 `COPY`/`SELECT` 输出为黄金对照。

### 6.5 L4 数据字典（含 LSN 时间线与替身）

- 构建（连源库，psycopg2）：`pg_class`（relfilenode 用 `pg_relation_filenode()`、relkind、reltoastrelid、replident）、`pg_attribute`（attnum/atttypid/atttypmod/attndims/attisdropped）、`pg_type`（typlen/typalign/typbyval/typelem/typcategory）、`pg_enum`、namespace、`pg_database`、tablespace；记录 `server_version` 与构建时 `pg_current_wal_lsn()`；
- **时间线**：字典可加载多份，每份带 `[start_lsn, end_lsn)` 有效区间；解析时按记录 LSN 选档案；区间冲突在 GUI 预检中告警。解决 VACUUM FULL/CLUSTER/SET TABLESPACE/TRUNCATE 引发的 relfilenode 分叉（需求 7 主路径）；
- **替身（avatar，学习 walminer）**：GUI/CLI 提供"新建替身表结构 → 映射到丢失 relfilenode"，旧 WAL 即可解析；
- JSON 导入导出（隔离环境，继承旧项目能力）；
- 旧项目 5 份真实字典全部纳入迁移兼容测试：aphx（唯一与现存 WAL 配对，参与端到端验证）+ tz 系列 4 份（WAL 已删，仅字典导入/合并层测试，见 §9.4）。

### 6.6 L5 SQL 生成器（可执行性保证 + 精简化，v1.3 按用户需求修订）

**行定位（WHERE）策略——优先级**：

1. **主键定位**（字典 `pk_attnums` 非空且元组中主键值可得）：`WHERE pk = v [AND ...]`——
   DELETE 的 DO、INSERT 的 UNDO（撤销插入=按主键删）均只按主键匹配，不拼接全部字段；
2. **全行匹配回退**（无主键信息）：`WHERE 全列 IS NOT DISTINCT FROM ...`（NULL 安全）。

**UPDATE 增量原则（DO 与 UNDO 对称）**：

- DO：`UPDATE t SET <仅变更列=新值> WHERE <旧行定位>`；
- UNDO：`UPDATE t SET <仅变更列=旧值> WHERE <新行定位>`（同一组变更列，值互换）；
- 变更列判定：新旧值逐列比较（None==None；类型一致精确比较），未变化的列不进 SET；
- 若 UPDATE 未变更任何列（如 `SET x=x`）：跳过该语句并记 notes（重放语义安全）；
- 若主键本身被更新：DO 用旧主键 WHERE + SET 新主键；UNDO 反之（天然正确）。

**其余不变**：INSERT 的 DO 全列插入；DELETE 的 UNDO 全列插回（恢复需要完整行）；
UNDO 按 LSN 降序；导出附 manifest；"待人工复核"独立分区。

### 6.7 L6 结果存储（对齐 walminer_contents）

```sql
-- pgwinalnew_contents（SQLite）
sqlno, xid, topxid, op, commit_ts, start_lsn, commit_lsn,
schema_name, table_name, relfilenode, block_num, offset_num,
row_data,        -- JSON 新值
old_row_data,    -- JSON 旧值
do_sql, undo_sql,
undo_source,     -- record|fpi|history|incomplete
executable,      -- §1.2 五条件
complete,        -- 事务边界是否完整（对齐 walminer complete）
notes
-- 辅助表: parse_runs / wal_files / unresolved_relations / fpi_pages(可选)
```

### 6.8 L7 GUI（继承旧布局，PySide6 重写）

**继承 pgwinal 布局约定**（需求 8，来自其 DEVELOPMENT.md §2.3）：

- 顶栏双行：品牌 + 统计摘要 + 主按钮「开始解析」；第二行：添加文件/目录、清空、字典三件套（生成/导入/导出）、导出 DO/UNDO/Excel；
- 左侧：WAL 文件列表 + 操作类型过滤 + 表名过滤；
- 中部：结果表（按操作着色，双击详情）；
- 底部：活动日志 + 进度条 + 状态栏；默认 1440×900，浅色主题。

**新增**（针对旧版痛点）：

| 面板 | 功能 |
|------|------|
| 预检对话框 | 段连续性/时间线一致性/版本探测/段大小/CRC 抽检报告，问题一键定位 |
| 字典时间线面板 | 多快照列表 + LSN 区间 + 冲突告警 + 替身映射管理 |
| 解析选项 | 时间窗/LSN 窗/xid（对齐 walminer 参数）+ 精确解析开关 + only_committed |
| 详情对话框 | 新旧值并排 + 原始字节 hex 视图 + undo_source 徽章 + executable 原因 |
| 导出向导 | 范围选择（表/时间/xid）→ 可执行/待复核分区 → do/undo/xlsx |

技术：QTableView + QSortFilterProxyModel（百万行虚拟滚动，解决 tkinter Treeview 瓶颈）；QThread 解析线程可取消，进度按字节。

---

## 7. PG 12–18 版本支持策略

### 7.1 差异管理方法（核心创新点：常量生成）

`tools/gen_constants.py`：拉取各版本源码 tag → 解析 `xlogrecord.h` / `heapam_xlog.h` / `rmgrlist.h` / `heapam_xlog.h` 相关宏 → 生成 `pgwal/profiles/v12..v18.py`。**人只写解析器逻辑，不手抄任何数值**。CI 中以头文件为黄金源校验。

### 7.2 版本差异矩阵（已对照 PG 12–18 官方 `heapam_xlog.h`/`xlogrecord.h` 逐项核实）

**稳定不变（12–18 全系一致，已验证）**：

| 项 | 值 |
|----|----|
| `XLogRecord` 头 | 24B：`tot_len u32 / xid u32 / prev u64 / info u8 / rmid u8 / pad 2B / crc u32` |
| Heap rmgr opcode | INSERT=00 DELETE=10 UPDATE=20 TRUNCATE=30 HOT_UPDATE=40 CONFIRM=50 LOCK=60 INPLACE=70（OPMASK=0x70，INIT_PAGE=0x80） |
| `XLOG_HEAP2_MULTI_INSERT` | **0x50（全系不变）** |
| `xl_heap_update` | **14B 含 `old_xmax`（全系不变）**：`old_xmax u32 / old_offnum u16 / old_infobits u8 / flags u8 / new_xmax u32 / new_offnum u16` |
| `xl_heap_delete` | 8B：`xmax u32 / offnum u16 / infobits u8 / flags u8` |
| `xl_heap_insert` / `xl_multi_insert_tuple` | 3B / 7B，布局不变 |
| 块头/镜像头 | 4B（`id/fork_flags/data_length`）；镜像头 5B（**`length u16 / hole_offset u16 / bimg_info u8`**）+ 可选压缩头 2B（`hole_length`） |
| 块 ID | DATA_SHORT=255 DATA_LONG=254 ORIGIN=253 TOPLEVEL_XID=252；XLR_MAX_BLOCK_ID=32 |

**分版本差异（差异点全部集中在 Heap2 opcode 与 prune/freeze 结构）**：

| Heap2 opcode | 0x00 | 0x10 | 0x20 | 0x30 | 0x40 | 0x60 | 0x70 |
|---|---|---|---|---|---|---|---|
| PG 12–13 | REWRITE | CLEAN | FREEZE_PAGE | CLEANUP_INFO | VISIBLE | LOCK_UPDATED | NEW_CID |
| PG 14–16 | REWRITE | PRUNE | VACUUM | FREEZE_PAGE | VISIBLE | LOCK_UPDATED | NEW_CID |
| PG 17–18 | REWRITE | PRUNE_ON_ACCESS | PRUNE_VACUUM_SCAN | PRUNE_VACUUM_CLEANUP | VISIBLE | LOCK_UPDATED | NEW_CID |

其他已核实的分版本差异：

| 版本 | 差异 |
|------|------|
| PG14 | `xl_heap_clean` 拆分为 `xl_heap_prune`（8B）+ `xl_heap_vacuum`（2B）；新增 `XLH_INSERT_ON_TOAST_RELATION(1<<4)`、`XLH_INSERT_ALL_FROZEN_SET(1<<5)` |
| PG15 | FPI 压缩方法位：`BKPIMAGE_COMPRESS_PGLZ=0x04 / LZ4=0x08 / ZSTD=0x10`（PG12–14 为单一 `IS_COMPRESSED=0x04`，仅 pglz） |
| PG16 | `xl_heap_prune` 重构（`snapshotConflictHorizon` + `isCatalogRel`）；freeze 改为 plans 结构；`xl_heap_lock` 字段微调（`locking_xid`→`xmax`，`int8`→`uint8`）；RelFileNode→RelFileLocator 改名（逻辑不变） |
| PG17 | **`xl_heap_prune` 彻底重设计**（`reason+flags` 2B + 可选非对齐 conflict horizon；块数据为 freeze plans / redirected / dead / unused 子记录序列，需实现 `heap_xlog_deserialize_prune_and_freeze` 等价物）；FREEZE_PAGE 记录取消（并入 PRUNE）；**UPDATE 记录最多 4 个块引用**（新增 VM_NEW/VM_OLD，`HEAP_UPDATE_BLKREF_*` 常量） |
| PG18 | 与 17 基本一致；`xl_heap_inplace` 扩展（dbId/tsId/共享失效消息） |

> 帧级（记录头/页头/块头）在 12–18 完全稳定，堆层差异全部集中在上述清单——**"一工具解析所有版本"的可行性已被官方头文件直接证实**，也与 walminer 3.x 单代码库 `#if PG_VERSION_NUM` 覆盖 10–19 的做法互相印证。上表将作为 `gen_constants.py` 的验收基准（生成物必须与此一致，不一致即脚本有 bug）。

### 7.3 版本探测

`pg_control`（若提供 PGDATA）→ 字典 server_version → CRC 引导探测（候选档案试扫描，首个连续通过者胜出）。

---

## 8. 结构变化（DDL）应对方案（需求 7 专项）

### 8.1 场景分类与策略

| 场景 | WAL 侧表现 | 策略 | 可恢复性 |
|------|-----------|------|----------|
| ADD COLUMN（PG11+ 快默认，不重写） | 元组布局不变 | 新字典直接解，缺列按默认值/NULL | ✅ 完整 |
| DROP COLUMN | pg_attribute 留 attisdropped 占位 | 字典含占位列，按 NULL 解 | ✅ 完整 |
| ALTER TYPE / VACUUM FULL / CLUSTER / SET TABLESPACE | relfilenode 变更，旧 WAL 指向旧 node | ① 旧时点字典快照（时间线）② 替身映射 | ✅（有旧字典/已知旧结构）/ ⚠️（仅 hex） |
| DROP TABLE / TRUNCATE | 旧 node 无任何当前映射 | 替身表（同结构新建映射旧 node）；TRUNCATE 前数据可从 FPI/历史流挽回（P2/P3） | ⚠️ 尽力 |
| 字典时间窗外的 DML | relfilenode 无法解析 | 进 `unresolved_relations` 表 + 报告（**不静默丢弃**，优于 walminer 的 notice-only） | ❌ 明示不可恢复 |

### 8.2 字典时间线（主路径）

多份不同时点字典 → LSN 区间档案 → 按记录 LSN 路由。GUI 预检时输出"结构变化时间轴报告"：哪些 relfilenode 在何时消失/出现，哪些 WAL 区间需要补充哪个时点的字典。

### 8.3 替身解析（学习 walminer `walminer_table_avatar`）

用户知晓旧表结构（建表语句/旧 DDL 脚本）时，GUI 引导"贴建表 SQL → 映射 relfilenode → 解析"。

### 8.4 目录自举（研究项，P3，差异化亮点）

pg_class(oid 1259)/pg_attribute(1249)/pg_type 等核心目录表**布局固定且已知**——可以像普通表一样从 WAL 解码其 heap 记录。据此可在解析过程中**自动发现** DDL（新 relfilenode 出现=建表/重写、pg_attribute 行增删=加/删列），自动扩展字典时间线，甚至从 WAL 自举出整个字典。这是超越 walminer 的方向，列为研究里程碑，不承诺交付。

### 8.5 诚实边界（写进产品文档）

- WAL 不含 ALTER TABLE 语句，工具不还原 DDL；
- 无任何旧结构信息（旧字典/替身）时，结构变化前的旧值只能 hex 呈现；
- 与 walminer 一致的物理限制，不承诺魔法。

---

## 9. 验证体系（防失败保险，本项目最高优先级工程）

### 9.1 真实实例测试矩阵（CI 就绪）

`tests/real_matrix/`：脚本化管理 12–18 各版本实例（Windows 便携 zip 或 Docker），每版本跑固定剧本。**优先级：PG12 最先建**（生产环境即 PG12，且真实数据只覆盖此版本），13–18 随后补齐：

- 剧本集：insert / update(同页+跨页+HOT) / delete / copy(MULTI_INSERT) / rollback / 大事务跨段 / TOAST 大值 / 各压缩配置 / checkpoint+FPI / REPLICA IDENTITY 三档 / DDL 前后（加列/删列/改类型/drop/truncate）；
- 断言：工具输出 SQL == 地面真值（脚本已知操作序列）；UNDO 回放后行状态 == 操作前状态。

### 9.2 pg_waldump 对照（帧级金标准）

PG Windows 发行版自带 `pg_waldump.exe`。CI 对每个测试 WAL 段跑 pg_waldump，与 L0/L1 输出逐条比对：记录数、LSN 序列、xid、rmid、info。**M0 退出标准 = 100% 一致**。工具内亦提供"预检"模式调用用户本机 pg_waldump（可选）。

### 9.3 黄金样本（单元级）

从真实矩阵截取小段 WAL（含各 opcode/边界情形）作为二进制 fixture 入库，单测毫秒级回归；布局常量变更即报警。**另有现成黄金用例：walminer 3.x 的 14 组回归测试（`reference/XLogMiner/walminer/sql/*.sql` 剧本 + `expected/*.out` 期望输出，覆盖 base/subxid/toast/spill/missimage/avatar/type/singletable 等场景），直接改造为本项目的对照测试**——这些是 walminer 作者在生产中验证过的行为基准。

### 9.4 现有真实数据（已实测核实，2026-09）

**唯一可用的真实端到端配对**：

| 项 | 值（实测） |
|----|-----------|
| WAL | `D:\mimo\pgwinal\testpg\`，**46 段 × 16MB ≈ 736MB**，timeline 1（`000000010000012B000000A0` 起），blcksz 8192，段大小 16MB |
| 字典 | `pgwinal_dict_aphx.sqlite`：PG **12.22**（Debian）、857 关系 / 17382 属性、db_oid 16384、含 `pk_attnums` 列、构建于 2026-09-18 |
| 配对验证 | WAL 段头 `xlp_sysid = 7579057487605995718` **与字典 meta.system_id 完全一致**（已实测）——这正是 §6.8 预检功能要做的事，配对天然成立 |
| 页头实测 | magic=**0xD101**（12–18 页头布局未变的实证）；首段 info 含 `XLP_FIRST_IS_CONTRECORD`——**该批 WAL 从流中途开始，第一条记录即为跨段续记录**，扫描器必须正确处理"孤儿续记录"（跳过 rem_len 字节），M0 的真实用例 |

使用注意：

1. **真实数据只覆盖 PG12** → §9.1 的自建实例矩阵（13–18）不可省略，是版本覆盖的唯一来源；
2. 字典构建时间（2026-09-18）晚于 WAL 中部分时段属正常生产情形——若 WAL 时段内发生过 DDL，`unresolved_relations` / 字典时间线路径将被真实触发，**这是检验需求 7 的天然素材**，预检报告应输出未解析 relfilenode 统计；
3. 其余 4 份字典（`pgwinal_dict` / `tz_kqdz` / `tz_rjxm` / `tz_tzwalechat_25`，同一库 PG12.16 按不同 schema 过滤）**配套 WAL 已删除**：仅用于字典导入/迁移/合并的兼容性测试（如同库多字典合并、schema 过滤字典的边界行为），不参与解析验证；
4. 736MB 体量适中：可作 M2/M3 的性能与内存基线（页像重放引擎 LRU、spill 阈值的真实压测输入）。

---

## 10. 里程碑计划

| 里程碑 | 内容 | 退出标准 | 预估 |
|--------|------|----------|------|
| **M0 正确性地基** | 仓库脚手架；gen_constants.py 生成 12–18 档案（**以 §7.2 已核实矩阵为验收基准**）；CRC32C；L0/L1 严格扫描器；pg_waldump 对照工具 | 真实 WAL（testpg 46 段，**含中途起始孤儿续记录用例**）帧级 100% 一致；system_id 预检可用；版本探测可用 | 1 周 |
| **M1 主路径** | L2 记录级解码 + 元组 deform + L3 核心 30 类型 + 字典构建；INSERT/DELETE 的 DO/UNDO；结果表 + CLI | **aphx 真实配对**（46 段 + 字典）上 INSERT/DELETE 可执行 SQL 抽样人工验证通过；walminer 回归剧本 base/type 移植通过 | 2 周 |
| **M2 完整 DML** | UPDATE 全变体（旧元组/旧键/prefix-suffix）、MULTI_INSERT（含 offsets[]）、事务过滤、TOAST；可执行性分级管线；导出 | 9.1 矩阵 **PG12（生产版本，优先）+ PG16（现代代表）双全剧本通过**（DO/UNDO == 真值）；aphx 真实配对全量解析无崩溃 + 抽样核对；walminer 剧本 subxid/toast/spill 移植通过 | 2 周 |
| **M3 页像重放与字典时间线** | 页像重放引擎（FPI 播种 + DML/prune redo + 精确解析模式）；字典时间线 + 替身；PySide6 GUI（继承布局） | replica 级 UPDATE/DELETE UNDO 重建率报告；walminer 剧本 missimage/avatar 移植通过；GUI 全流程可操作 | 2 周 |
| **M4 版本矩阵与交付** | 12–18 实测矩阵补齐；DDL 场景验证；PyInstaller 打包；文档 | 全版本矩阵绿；单文件 exe 在干净 Windows 跑通端到端 | 1–2 周 |
| **M5 增强（可选）** | 并行解析；TRUNCATE 数据挽回；目录自举研究；Rust 内核评估 | 按需 | — |

> **M0 是去风险关卡**：若 1 周内真实 WAL 帧级对照无法达到 100%，立即停下评估切换 B1（C 移植）路线，避免重蹈旧项目覆辙。

---

## 11. 可行性评估

### 11.1 技术风险登记册

| 风险 | 概率 | 影响 | 缓解 |
|------|------|------|------|
| 12–18 布局差异比预期大 | 中 | 高 | 生成式常量 + 每版本真实实例矩阵；M0 早暴露 |
| TOAST/FPI 压缩编解码（pglz/lz4/zstd） | 中 | 中 | pglz 纯 Python 可靠实现；lz4/zstd 成熟 pip 包；失败降级标记不猜值 |
| UNDO 完整性受源库配置制约 | 高（物理定律） | 中 | §4 约束矩阵前置告知 + FPI/历史重建 + undo_source 标注 |
| 性能不足（大 WAL） | 低 | 中 | 段级并行；crc32c C 加速；实测目标 ≥15MB/s/核；Rust 内核逃生通道 |
| DDL 场景覆盖不及预期 | 中 | 中 | 时间线+替身为主路径；目录自举为研究项；诚实边界文档化 |
| 范围蔓延（做成通用 WAL 浏览器） | 中 | 中 | 里程碑锁定 DML 恢复主线，增强项全部 P2/P3 |

### 11.2 性能预估（诚实数字）

- 纯 Python 表驱动 CRC32C ≈ 20–40 MB/s；`crc32c` C 扩展 ≈ 200+ MB/s；元组解码为另一热点；
- 单核综合目标 ≥ 15 MB/s：16MB 段 ≈ 1–2 秒；1GB WAL（约 64 段）单核 ≈ 1–2 分钟，8 核并行 ≈ 10–20 秒；
- 恢复场景（数段~数 GB）完全够用；重放全量归档属边缘场景 → M5 并行/Rust。

### 11.3 工作量与总体判定

- **总量**：M0–M4 约 8–12 人周（熟悉 PG 内部者取下限；含测试矩阵搭建）；
- **判定**：**可行，且风险较初版评估进一步下降**。理由：① 物理格式公开且有 walminer/pg_waldump 双参照；② **walminer 3.x MIT 源码在本地可供逐函数对照**（页像重放引擎、字典、SQL 生成的算法不用从零发明）；③ **PG 12–18 堆层版本差异矩阵已对照官方头文件逐项核实**（§7.2，差异全部集中在 Heap2 opcode 与 prune 结构，帧级全系稳定）；④ walminer.cn 闭源版已证明 PG10–18 商业级可用，市场空缺（Windows/开源）真实存在；⑤ 旧项目失败根因全部可识别、可规避（方法论问题而非方向问题）；⑥ 验证体系（§9）把最大风险（静默错误）变成显性失败；
- **成功的关键不在算法，而在纪律**：CRC 严格、常量生成、真值对照、不确定即标记。这四条纪律写进 CI 与代码评审清单。

---

## 12. 目录结构规划

```
D:\mimo\pgwinalnew\
  README.md
  开发方案.md                  ← 本文档
  requirements.txt
  run_gui.py / pgwalnew.exe(打包产物)
  tools/
    gen_constants.py           ← 从 PG 官方头文件生成版本档案
    waldump_diff.py            ← 与 pg_waldump 逐条对照
  pgwalnew/
    profiles/ v12.py ... v18.py   (生成物)
    core/    segment.py crc32c.py xlogreader.py heaptuple.py
    decode/  heap.py heap2.py xact.py toast.py fpi.py
    types/   registry.py literals.py
    dictstore/ builder.py timeline.py avatar.py store.py
    sqlgen/  dogen.py undogen.py manifest.py
    resultstore/ db.py xlsx.py
    engine.py                  ← Pass0/1/2 编排
    cli.py  gui/
  tests/
    unit/  fixtures/           ← 黄金二进制样本
    real_matrix/               ← 12–18 实例剧本与断言
  testdata/
    wal_segments/              ← 迁入旧项目 testpg 46 段（aphx 配对，736MB，PG12）
    dictionaries/
      aphx/                    ← pgwinal_dict_aphx.sqlite（唯一与现存 WAL 配对的字典）
      tz_series/               ← 其余 4 份（WAL 已删，仅字典导入/合并兼容测试）
  reference/
    XLogMiner/                 ← walminer 3.x 源码（MIT，已 clone，算法参考+回归测试）
    pg_headers/                ← gen_constants.py 拉取的各版本官方头文件快照
```

---

## 附录 A：二进制布局速查（已对照 PG 12–18 官方头文件逐项验证）

> 验证方式：直接核对 postgres/postgres 官方仓库 REL_12/13/14/15/16/17/18_STABLE 的 `heapam_xlog.h` 与 `xlogrecord.h` 原文（2026-09）。M0 的 gen_constants.py 生成物必须与此表一致。

| 结构 | 布局 | 版本适用 |
|------|------|----------|
| XLogRecord（24B） | `tot_len u32 / xid u32 / prev u64 / info u8 / rmid u8 / pad 2B / crc u32` | 12–18 不变 |
| 短页头（24B） | `magic u16 / info u16 / tli u32 / pageaddr u64 / rem_len u32 / pad 4B`（magic=**0xD101**，rem_len 在 offset 16；标志位 `XLP_FIRST_IS_CONTRECORD=0x01 / XLP_LONG_HEADER=0x02`） | 12–18 不变 |
| 长页头（40B） | 短头 + `sysid u64 / seg_size u32 / blcksz u32` | 12–18 不变 |
| 块头（4B） | `id u8 / fork_flags u8 / data_length u16`；高 4 位 HAS_IMAGE=0x10 HAS_DATA=0x20 WILL_INIT=0x40 SAME_REL=0x80 | 12–18 不变 |
| 镜像头（5B） | **`length u16 / hole_offset u16 / bimg_info u8`**；HAS_HOLE=0x01；压缩位：**12–14 `IS_COMPRESSED=0x02, APPLY=0x04`；15+ `APPLY=0x02, PGLZ=0x04, LZ4=0x08, ZSTD=0x10`（0x02/0x04 语义在 PG15 前后互换——实测踩坑）**；压缩且 HAS_HOLE 时后跟 `hole_length u16`，未压缩时洞长=BLCKSZ−length 可推导 | 压缩位 15 起变化 |
| main 标记 | 0xFF+u8 len（短）；0xFE+u32 len（长）；0xFD origin；0xFC toplevel xid；XLR_MAX_BLOCK_ID=32 | 12–18 不变 |
| HeapTupleHeader（23B） | `xmin 4 / xmax 4 / cid 4 / ctid 6 / infomask2 2 / infomask 2 / hoff 1` + nullbitmap + data | 12–18 不变 |
| xl_heap_header（5B，WAL 内元组头） | `t_infomask2 u16 / t_infomask u16 / t_hoff u8` | 12–18 不变 |
| varlena 标签 | 首字节（小端）：`低2位=00` 4B未压缩（size=u32>>2）；`低2位=10` 4B压缩（后随 u32 rawsize + pglz）；`奇数` 1B（size=byte>>1）；`==0x01` 1B外部（TOAST 指针，后随 tag 字节）；varlena 列对齐先窥视当前字节，非零不对方、为零对齐 4 | 12–18 不变 |
| Heap opcode（info&0x70） | INSERT=00 DELETE=10 UPDATE=20 TRUNCATE=30 HOT_UPDATE=40 CONFIRM=50 LOCK=60 INPLACE=70；INIT_PAGE=0x80 | 12–18 不变 |
| Heap2 opcode | MULTI_INSERT=**0x50** / LOCK_UPDATED=0x60 / NEW_CID=0x70 / REWRITE=0x00 / VISIBLE=0x40 全系不变；0x10/0x20/0x30 见 §7.2 分版本矩阵 | 分版本 |
| xl_heap_update（14B） | **`old_xmax u32 / old_offnum u16 / old_infobits u8 / flags u8 / new_xmax u32 / new_offnum u16`（含 old_xmax）** | 12–18 不变 |
| xl_heap_delete（8B） | `xmax u32 / offnum u16 / infobits u8 / flags u8` | 12–18 不变 |
| xl_heap_insert（3B） | `offnum u16 / flags u8` | 12–18 不变 |
| xl_heap_multi_insert | `flags u8 / ntuples u16 / offsets[] u16×n`（INIT_PAGE 时 offsets 省略，offnum=i+1） | 12–18 不变 |
| xl_multi_insert_tuple（7B） | `datalen u16 / t_infomask2 u16 / t_infomask u16 / t_hoff u8` + data，逐条 SHORTALIGN | 12–18 不变 |
| UPDATE 块引用 | blk0=新页（数据），blk1=旧页（如有）；**PG17+ 可再有 blk2/blk3=VM 页（无数据，仅引用）** | 17 起扩展 |
| CRC | CRC32C（Castagnoli），整条记录，xl_crc 字段以 0 参与计算 | 12–18 不变 |

## 附录 B：参考资料

- **walminer 3.x 源码（MIT，已 clone）**：https://gitee.com/movead/XLogMiner → 本地 `reference/XLogMiner/`（walminer_decode.c / imagemanage.c / datadictionary.c / wal2sql.c / expected 回归测试）
- **Walminer 4.x 官网（闭源，PG10–18，Linux）**：http://walminer.cn/
- walminer GitHub 镜像与 README：https://github.com/universe2000/walminer
- walminer 3.0/4.0 详解（digoal）：https://github.com/digoal/blog/blob/aded86da45dd5b3f0f0e0634ae522f394277c2f5/202211/20221102_01.md
- PostgreSQL 源码（各版本 tag，本方案附录 A 的验证依据）：https://github.com/postgres/postgres （REL_12~18_STABLE 的 xlogrecord.h / heapam_xlog.h / heapam.c / xlogreader.c）
- pg_waldump（帧级对照金标准，Windows 发行版 bin 内自带）
- 旧项目交接文档：`D:\mimo\pgwinal\docs\DEVELOPMENT.md`

---

**一句话总结**：pgwinal 的失败是"无校验 + 手抄常量 + 合成自测 + 启发式兜底"四重方法论错误的必然结果（本方案初版自己也在两处常量上凭记忆犯错，靠官方头文件才纠正——教训加倍成立）；pgwinalnew 以"CRC 严格校验、常量生成、真值对照、不确定即标记"四条纪律重建，以 walminer 3.x MIT 源码为算法参照（页像重放引擎 + 字典/替身），PG12–18 差异矩阵已对照官方头文件逐项核实，walminer.cn 闭源版证明 10–18 商业可用而 Windows+开源是空缺——**项目可行，风险可控，M0 一周内即可用真实数据验证地基是否牢靠**。
